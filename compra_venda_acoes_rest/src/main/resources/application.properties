server.port=8085

spring.jackson.serialization.fail-on-empty-beans=false
logging.level.org.springframework.web=DEBUG

spring.data.rest.base-path=/api_rest
spring.jpa.database=POSTGRESQL
spring.datasource.platform=postgres
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto=update
spring.database.driverClassName=org.postgresql.Driver

#Crie antes, no PgAdmin, um banco de dados chamado: acoes_db
spring.datasource.url=jdbc:postgresql://localhost:5432/acoes_db
# Mude o nome do usuário e a senha para no seu PostgreSQL.
spring.datasource.username=postgres
spring.datasource.password=Senh4[P0st]
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false
spring.jpa.properties.hibernate.default_schema=public

spring.datasource.initialization-mode=always
